# TOOLS.md - Environment Configuration v2.1 **Last Updated:** 2026-04-01 **Purpose:** Alfred CamHer - HKUDS Stack Integration --- ## 🦞 HKUDS Stack (Estado Actual) ### Playwright **Estado:** ✅ Instalado (v1.58.0) + Chromium **Instalación:** `pip3 install --user --break-system-packages playwright && playwright install chromium` **Uso:** Browser automation, screenshots, web scraping **Ejemplo:** ```python from playwright.sync_api import sync_playwright with sync_playwright() as p: browser = p.chromium.launch() page = browser.new_page() page.goto('https://relaylabs.com') page.screenshot(path='relaylabs-screenshot.png') browser.close() ``` --- ### LightRAG **Estado:** ✅ Instalado (v1.4.13) + Ollama backend **Instalación:** `pip3 install --user --break-system-packages lightrag-hku` **Uso:** Query graph-based RAG sobre memoria **Script:** `/home/alfredcamher/.openclaw/tools/setup-lightrag.py` **Comandos:** ```bash # Indexar memoria python3 ~/.openclaw/tools/setup-lightrag.py index # Query python3 ~/.openclaw/tools/setup-lightrag.py query "token optimization strategies" ``` **WorkingDir:** `~/.openclaw/knowledge/` --- ### CLI-Anything **Estado:** ✅ Cloned + operacional (plugin-based) **Path:** `~/.openclaw/tools/CLI-Anything/` **Estructura:** Plugins en directorios `CLI-Anything/<tool>/` **Uso:** Wrappers para ejecutar ffmpeg, pandoc, etc. **PDF Tool:** `/home/alfredcamher/.openclaw/tools/run-pandoc-pdf.py` (wrapper independiente) --- ### RAG-Anything **Path:** `~/.openclaw/tools/rag-anything/` **Status:** Cloned **Uso:** RAG multimodal (PDFs, imágenes) --- ### DeepCode **Path:** `~/.openclaw/tools/deepcode/` **Status:** Clonado - instalación pendiente **Uso:** Generate code from papers --- ## Herramientas Operativas Diarias ### Brave Search **API Key:** Configurado en entorno **Límite:** 300 req/día **Costo:** $0 (free tier) ### Ollama **Modelo:** llama3.2:3b **Puerto:** 11434 **Estado:** Running (usado por LightRAG) **Comando:** `ollama serve &` --- ## Cron Jobs Configurados ### LightRAG Daily Index - **Schedule:** `0 2 * * *` (2 AM daily) - **Action:** Indexar archivos markdown en `memory/` - **Script:** `setup-lightrag.py index` ### PDF Auto-generation - **Trigger:** On-demand / cron - **Tool:** `run-pandoc-pdf.py` - **Requisitos:** `pandoc` + `texlive-xetex` (sin sudo) --- ## Capacidades Expandidas | Tool | Mi Uso | Beneficio | |------|--------|-----------| | Playwright | Screenshots, automation | $0 browser control | | LightRAG | Buscar knowledge base | 10x más rápido que búsqueda lineal | | CLI-Anything | Ejecutar ffmpeg, pandoc | Cualquier CLI | | RAG-Anything | Analizar PDFs multimodal | Entender imágenes + texto | --- ## Scripts Rápidos ### PDF desde Markdown ```bash python3 ~/.openclaw/tools/run-pandoc-pdf.py CEO-AUTONOMO-GUIA-COMPLETA.md -o guia.pdf --pages ``` ### LightRAG Query ```bash python3 ~/.openclaw/tools/setup-lightrag.py query "marketing strategy GTM" ``` ### Playwright Screenshot ```bash python3 -c "from playwright.sync_api import sync_playwright; \
  p=sync_playwright().start(); b=p.chromium.launch(); \
  page=b.new_page(); page.goto('https://buy.stripe.com/aFabJ28NrfiHdFl3jfcMM00'); \
  page.screenshot(path='stripe-checkout.png'); b.close(); p.stop()" ``` --- ## Workflow Automatizado ### Morning Routine (9 AM) 1. LightRAG → index overnight changes 2. Playwright → check payment page health 3. Analytics review ### Coding Sessions 1. DeepCode assist (cuando esté listo) 2. CLI-Anything para tools externos ### Research Tasks 1. Brave API for web search 2. LightRAG para retrieval rápido 3. Playwright para extraer data de páginas --- ## Próximos Pasos - [ ] Instalar DeepCode completamente - [ ] Configurar cron job auto-index cada 2 horas - [ ] Crear Playwright script para automatizar screenshot deployment - [ ] Añadir RAG-Anything configuración multimodal --- *Tools v2.1 | Playwright: ✅ | LightRAG: ✅ | CLI-Anything: ✅ | DeepCode: ⏳*
